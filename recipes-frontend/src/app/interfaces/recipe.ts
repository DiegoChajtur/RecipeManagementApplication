export interface Recipe {
    id: Number;
    name: string;
    description: string;
    instructions: string;
    ingredients: string;
    tags: string;
    createAt: string;
    updateAt: string;
  }
